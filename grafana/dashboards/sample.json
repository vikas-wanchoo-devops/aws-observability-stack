{
  "dashboard": {
    "id": null,
    "title": "AWS Observability Dashboard",
    "panels": [
      {
        "type": "graph",
        "title": "ECS CPU Utilization",
        "targets": [
          { "expr": "aws_ecs_cpu_utilization_average" }
        ]
      },
      {
        "type": "graph",
        "title": "ECS Memory Utilization",
        "targets": [
          { "expr": "aws_ecs_memory_utilization_average" }
        ]
      },
      {
        "type": "graph",
        "title": "ALB Request Count",
        "targets": [
          { "expr": "aws_alb_request_count_sum" }
        ]
      }
    ]
  },
  "overwrite": true
}
